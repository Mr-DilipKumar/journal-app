/**
 * Supabase Configuration Template
 * 
 * Instructions:
 * 1. Copy this file and rename it to `config.js`
 * 2. Replace the placeholder values with your Supabase Project URL and Anon Public Key
 *    (Found in your Supabase project dashboard under Settings -> API)
 * 3. Notice: `config.js` is included in `.gitignore` so your keys won't be pushed to git!
 */

window.SUPABASE_CONFIG = {
  url: "https://your-project-id.supabase.co",
  anonKey: "your-anon-public-key-here"
};
